---
phase: 37-http-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/routing/http-handler.js
  - tests/contract-http-headers.test.js
  - tests/helpers/runtime-fixtures.js
autonomous: true
requirements:
  - HCON-01
  - HCON-02
must_haves:
  truths:
    - "Every JSON response emitted through Server A returns Content-Type with explicit UTF-8 charset."
    - "OPTIONS preflight responses return deterministic 204 envelopes with explicit Content-Length: 0."
    - "Allowed-origin preflight responses include Access-Control-Max-Age for browser-side preflight caching."
  artifacts:
    - path: "modules/routing/http-handler.js"
      provides: "Canonical JSON and preflight response header contract implementation."
      contains: "function sendJson"
    - path: "tests/contract-http-headers.test.js"
      provides: "Automated contract assertions for JSON and OPTIONS headers."
      contains: "test(\"OPTIONS preflight"
  key_links:
    - from: "tests/contract-http-headers.test.js"
      to: "modules/routing/http-handler.js"
      via: "request simulation through createHttpHandler"
      pattern: "createHttpHandler"
---

<objective>
Harden Server A JSON and preflight contracts so clients receive deterministic HTTP semantics for UTF-8 JSON and CORS preflight behavior.

Purpose: Close HCON-01 and HCON-02 with executable contract coverage before downstream reliability work.
Output: Updated `http-handler.js` response headers plus a new focused contract test file for header behavior.
</objective>

<execution_context>
@../.opencode/get-shit-done/workflows/execute-plan.md
@../.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-http-contract-hardening/37-CONTEXT.md
@.planning/phases/37-http-contract-hardening/37-RESEARCH.md
@../.planning/ROADMAP.md
@../.planning/REQUIREMENTS.md

<interfaces>
From `serverless.js`:
```javascript
const { createHttpHandler } = require("./modules/routing/http-handler");
module.exports = createHttpHandler;
```

From `modules/routing/http-handler.js`:
```javascript
function sendJson(req, res, statusCode, payload) { ... }
function handlePreflight(req, res) { ... }
async function createHttpHandler(req, res) { ... }
module.exports = { createHttpHandler };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: create Wave 0 header contract tests for JSON and OPTIONS</name>
  <files>tests/contract-http-headers.test.js, tests/helpers/runtime-fixtures.js</files>
  <action>Create a focused Node `node:test` contract file that simulates requests against `createHttpHandler` and asserts exact response headers for HCON-01 and HCON-02. Cover at minimum: (1) JSON success path returns `Content-Type: application/json; charset=utf-8`; (2) preflight allowed-origin response returns 204 + `Content-Length: 0` + existing allow-method/header contract + `Access-Control-Max-Age: 7200`; (3) blocked-origin preflight still returns 204 + `Content-Length: 0`. If a reusable request/response helper is needed, add it under `tests/helpers/runtime-fixtures.js`. Do not add network calls; keep tests local and deterministic.</action>
  <verify>
    <automated>node --test tests/contract-http-headers.test.js</automated>
  </verify>
  <done>Test file exists, fails before implementation changes, and explicitly encodes HCON-01/HCON-02 expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement UTF-8 JSON and deterministic preflight header contract</name>
  <files>modules/routing/http-handler.js, tests/contract-http-headers.test.js</files>
  <behavior>
    - Test 1: JSON responses from `sendJson` include `application/json; charset=utf-8`.
    - Test 2: Allowed preflight response returns 204 with `Content-Length: 0` and `Access-Control-Max-Age: 7200`.
    - Test 3: Blocked-origin preflight 204 response also includes `Content-Length: 0`.
  </behavior>
  <action>Update `sendJson()` to set `Content-Type` exactly to `application/json; charset=utf-8`. Update `handlePreflight()` to set `Content-Length: 0` for both 204 exit branches and add `Access-Control-Max-Age: 7200` on the allowed-origin 204 response while preserving existing `Access-Control-Allow-Methods` and `Access-Control-Allow-Headers` behavior. Keep the existing 403 error behavior for disallowed method/header preflights unchanged.</action>
  <verify>
    <automated>node --test tests/contract-http-headers.test.js</automated>
  </verify>
  <done>All HCON-01/HCON-02 tests pass and code paths now emit deterministic UTF-8 JSON and preflight headers.</done>
</task>

</tasks>

<verification>
- `node --test tests/contract-http-headers.test.js`
- `node "../.opencode/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/37-http-contract-hardening/37-01-PLAN.md" --schema plan`
- `node "../.opencode/get-shit-done/bin/gsd-tools.cjs" verify plan-structure ".planning/phases/37-http-contract-hardening/37-01-PLAN.md"`
</verification>

<success_criteria>
- JSON response contract includes explicit UTF-8 content type on success and error paths.
- OPTIONS preflight contract is deterministic (204, `Content-Length: 0`, stable allow headers, `Access-Control-Max-Age: 7200`).
- New contract tests lock both behaviors and run green in under 60 seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/37-http-contract-hardening/37-01-SUMMARY.md`
</output>
