---
phase: 37-http-contract-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - modules/routing/http-handler.js
  - tests/contract-http-headers.test.js
  - .planning/phases/37-http-contract-hardening/37-CONTRACT-EVIDENCE.md
autonomous: true
requirements:
  - HCON-03
must_haves:
  truths:
    - "Manifest responses expose an explicit cache policy instead of implicit platform defaults."
    - "Manifest cache behavior reduces repeated cold-fetch pressure without long stale contract windows."
    - "Phase output includes source-backed evidence proving the manifest cache contract exists and is test-locked."
  artifacts:
    - path: "modules/routing/http-handler.js"
      provides: "Manifest route header interception before runtimeRouter dispatch."
      contains: "pathname === \"/manifest.json\""
    - path: "tests/contract-http-headers.test.js"
      provides: "Automated assertion for manifest Cache-Control contract."
      contains: "Cache-Control"
    - path: ".planning/phases/37-http-contract-hardening/37-CONTRACT-EVIDENCE.md"
      provides: "Human-readable proof linking implementation to requirement and source rationale."
      contains: "HCON-03"
  key_links:
    - from: "modules/routing/http-handler.js"
      to: "stremio-addon-sdk runtimeRouter"
      via: "header set before runtimeRouter(req, res, next)"
      pattern: "runtimeRouter(req, res"
---

<objective>
Implement and verify explicit manifest cache-header hardening in Server A, then record requirement-level evidence for the phase contract.

Purpose: Close HCON-03 and leave auditable proof that manifest caching is explicit and bounded.
Output: Manifest header interception in runtime path, updated contract tests, and a phase evidence document.
</objective>

<execution_context>
@../.opencode/get-shit-done/workflows/execute-plan.md
@../.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-http-contract-hardening/37-CONTEXT.md
@.planning/phases/37-http-contract-hardening/37-RESEARCH.md
@.planning/phases/37-http-contract-hardening/37-01-SUMMARY.md
@../.planning/ROADMAP.md
@../.planning/REQUIREMENTS.md

<interfaces>
From `modules/routing/http-handler.js`:
```javascript
async function createHttpHandler(req, res) {
  const runtimeRouter = getRouter(getAddonInterface());
  // ...
  runtimeRouter(req, res, () => {
    res.statusCode = 404;
    res.end();
  });
}
```

From `addon.js`:
```javascript
const addonInterface = builder.getInterface();
module.exports = addonInterface;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add manifest cache-control interception and test coverage</name>
  <files>modules/routing/http-handler.js, tests/contract-http-headers.test.js</files>
  <behavior>
    - Test 1: GET `/manifest.json` response includes `Cache-Control: public, max-age=300, must-revalidate`.
    - Test 2: Non-manifest Stremio routes are not forced to use the manifest cache header.
  </behavior>
  <action>Extend `tests/contract-http-headers.test.js` with manifest cache assertions first, then update `createHttpHandler()` so when `pathname === "/manifest.json"`, it sets `Cache-Control` to `public, max-age=300, must-revalidate` immediately before `runtimeRouter` dispatch. Keep change local to manifest handling and do not move cache policy into `vercel.json` (per locked context decision).</action>
  <verify>
    <automated>node --test tests/contract-http-headers.test.js</automated>
  </verify>
  <done>Manifest route emits explicit cache header and regression tests lock the behavior without collateral route changes.</done>
</task>

<task type="auto">
  <name>task 2: capture requirement-to-evidence mapping for HTTP contract hardening</name>
  <files>.planning/phases/37-http-contract-hardening/37-CONTRACT-EVIDENCE.md</files>
  <action>Create a concise evidence report with sections for HCON-01, HCON-02, and HCON-03. For each requirement include: implementation file + function, exact header contract string, automated test name/command, and research rationale reference from `37-RESEARCH.md`. Add a short "How to re-verify" block with copy-paste commands. Keep this file phase-local and implementation-facing (no generic narrative).</action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/37-http-contract-hardening/37-CONTRACT-EVIDENCE.md';const t=fs.readFileSync(p,'utf8');['HCON-01','HCON-02','HCON-03','How to re-verify'].forEach((s)=>{if(!t.includes(s)) throw new Error('Missing '+s);});"</automated>
  </verify>
  <done>Evidence file exists and gives a direct requirement -> code -> test trace for all Phase 37 contracts.</done>
</task>

</tasks>

<verification>
- `node --test tests/contract-http-headers.test.js`
- `node "../.opencode/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/37-http-contract-hardening/37-02-PLAN.md" --schema plan`
- `node "../.opencode/get-shit-done/bin/gsd-tools.cjs" verify plan-structure ".planning/phases/37-http-contract-hardening/37-02-PLAN.md"`
</verification>

<success_criteria>
- Manifest route has explicit cache policy (`public, max-age=300, must-revalidate`) set in Server A runtime path.
- Contract tests verify HCON-01/02/03 header behavior end-to-end through handler execution.
- Phase-local evidence doc maps each requirement to concrete code and automated verification commands.
</success_criteria>

<output>
After completion, create `.planning/phases/37-http-contract-hardening/37-02-SUMMARY.md`
</output>
