# Plan 07-05: Nightly Rollup

## Goal

Aggregate hourly analytics into daily summaries with safe idempotent execution.

## Tasks

- Add lock acquisition for rollup execution.
- Enforce per-day idempotency metadata checks.
- Aggregate hourly counters and uniques.
- Write daily summary and clean rolled hourly keys.

## Definition of Done

- Rollup is safe under concurrent triggers.
- Re-running same day does not duplicate results unless explicitly forced.
