# Plan 07-01: Runtime Constants

## Goal

Parameterize analytics/session TTL and runtime controls for patch behavior.

## Tasks

- Add env-backed constants for session view TTL and hourly analytics TTL.
- Keep existing stream gate constants configurable and intact.
- Wire constants into routing/control dependency graph.

## Definition of Done

- Constants are read from env with safe defaults.
- Stream-path logic receives new TTL values through existing wiring.
