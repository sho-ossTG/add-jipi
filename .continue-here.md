---
phase: pre-phase-0
task: 0
total_tasks: 0
status: paused
last_updated: 2026-02-28T19:08:42.240Z
---

<current_state>
New project initialized via /gsd:new-project. All planning groundwork is complete. No code phases have started. Ready to plan Phase 0 (Prerequisites).
</current_state>

<completed_work>

- Ran /gsd:new-project for "Server A → D Integration Adaptation"
- Mapped existing codebase (.planning/codebase/ documents)
- Completed project research (STACK, FEATURES, ARCHITECTURE, PITFALLS, SUMMARY)
- Wrote REQUIREMENTS.md (15 requirements: PRE-1..3, FR-1..6, NFR-1..5)
- Wrote ROADMAP.md with 6 phases (Phase 0–5)
- STATE.md updated: "Not started (ready for Phase 0)"
- Old v1.0 and v1.0.1 planning phase directories removed from git tracking
</completed_work>

<remaining_work>

- Plan Phase 0: Prerequisites (wire test files, fix 3 rollup bugs, extract shared module)
- Execute Phase 0
- Plan and execute Phase 1: D Client Interface + Stub
- Plan and execute Phase 2: Wire Resolution Path
- Plan and execute Phase 3: User-Agent Forwarding
- Plan and execute Phase 4: Nightly Log Shipping
- Plan and execute Phase 5: Broker Deprecation
</remaining_work>

<decisions_made>

- Scope is integration-only: Server A → Server D, replacing broker for episode resolution
- D client uses native fetch (no new npm deps, NFR-1)
- Timeout budget: 5s attempt / 10s total (tighter than broker's 60s)
- Degradation label stays "broker" during Phases 1–4; renamed to "d" in Phase 5 only
- No fallback title parsing — if D returns no title, throw validation_error (clean failure)
- Fire-and-forget for UA forwarding and log shipping (never block stream response)
- Log shipping is non-destructive (LRANGE, rollup owns cleanup)
- Phase 5 has a manual production gate: D must be confirmed live before broker deletion
</decisions_made>

<blockers>
None.
</blockers>

<context>
This session initialized a new GSD project after v1.0.1 was archived. The project "Server A → D Integration Adaptation" adapts Server A to route episode resolution through Server D instead of calling the broker directly.

The 6 bugs identified in the previous session (inter-milestone-diagnostic) are addressed in the roadmap:
- Bug 1 (analytics gap in shutdown window) → Phase 0 PRE-2
- Bug 2 (HyperLogLog PFADD not written) → Phase 0 PRE-2
- Bug 3 (uniqueEstimateTotal always 0) → Phase 0 PRE-2
- Bug 4 (4 test files not wired) → Phase 0 PRE-1
- Bug 5 (stale mock key in runtime-fixtures.js) → Phase 0 or separate cleanup
- Bug 6 (ZADD mock conflates two sorted sets) → Phase 0 or separate cleanup

Note: Bugs 5 & 6 are test fixture issues. They're mentioned in the research/pitfalls docs but may need explicit task coverage in Phase 0 plan if not already captured.

Key stack facts:
- Node.js CommonJS, Stremio addon SDK, Redis REST (Upstash), Vercel serverless
- Session gate: system:active_sessions (sorted set)
- Episode share: episode:share:{episodeId} (string JSON, 30-min TTL)
- Hourly analytics: analytics:hourly (hash, field: {bucket}|{event}|{metric})
- Unique tracking: analytics:unique:{bucket} (HyperLogLog per bucket)
</context>

<next_action>
Run /gsd:plan-phase 0 to create the Phase 0 plan (Prerequisites).
This will plan: wiring 4 test files, fixing 3 rollup bugs, extracting executeBoundedDependency.
Also verify that Bugs 5 & 6 (test fixture issues) are addressed in the plan.
</next_action>
